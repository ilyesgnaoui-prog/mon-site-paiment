<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>SafePay — Paiement de démonstration</title>
  <style>
    :root{
      --bg:#f4f7fb;
      --card:#ffffff;
      --accent:#1f6feb;
      --muted:#6b7280;
      --success:#10b981;
      --shadow: 0 6px 18px rgba(20,30,60,0.08);
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      background:linear-gradient(180deg,#eef6ff 0%,var(--bg) 100%);
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:32px;
    }
    .container{
      width:100%;
      max-width:920px;
      display:grid;
      grid-template-columns: 1fr 420px;
      gap:28px;
      align-items:start;
    }
    .hero{
      background:var(--card);
      border-radius:14px;
      padding:28px;
      box-shadow:var(--shadow);
    }
    h1{margin:0 0 10px;font-size:22px}
    p.lead{margin:0 0 18px;color:var(--muted)}
    .features{display:flex;gap:10px;flex-wrap:wrap}
    .feature{
      background:#f8fbff;
      border-radius:10px;
      padding:10px 12px;
      font-size:14px;
      color:#0b2546;
      display:flex;
      gap:8px;
      align-items:center;
    }

    /* right column (form) */
    .card{
      background:var(--card);
      border-radius:14px;
      padding:22px;
      box-shadow:var(--shadow);
    }
    label{display:block;font-size:13px;color:var(--muted);margin-bottom:6px}
    .input{
      width:100%;
      padding:12px 14px;
      border-radius:8px;
      border:1px solid #e6eef8;
      font-size:15px;
      outline:none;
      margin-bottom:12px;
      background:transparent;
    }
    .row{display:flex;gap:12px}
    .small{flex:1}
    .btn{
      display:inline-block;
      padding:12px 16px;
      border-radius:10px;
      background:var(--accent);
      color:#fff;
      border:none;
      cursor:pointer;
      font-weight:600;
      width:100%;
      margin-top:6px;
      box-shadow:0 6px 12px rgba(31,111,235,0.12);
    }
    .muted{font-size:13px;color:var(--muted)}
    .total{
      display:flex;
      justify-content:space-between;
      align-items:center;
      margin-top:14px;
      font-weight:700;
      font-size:18px;
    }
    .notice{
      margin-top:10px;
      font-size:13px;
      color:#374151;
      background:#f9fbff;
      padding:10px;border-radius:8px;border:1px solid #eef6ff;
    }
    .center{display:flex;align-items:center;justify-content:center}
    /* simple success modal */
    .modal{
      position:fixed;inset:0;
      display:none;
      align-items:center;justify-content:center;
      background:rgba(8,15,30,0.35);
    }
    .modal .box{
      background:var(--card);
      padding:22px;border-radius:12px;
      width:min(520px,92%);
      box-shadow:var(--shadow);
      text-align:center;
    }
    .ok{
      display:inline-block;
      width:64px;height:64px;border-radius:999px;
      background:linear-gradient(180deg,#e6fbf2,#ddf7ee);
      color:var(--success);font-size:30px;line-height:64px;margin:0 auto 12px;
    }
    .small-muted{font-size:13px;color:var(--muted)}
    footer{margin-top:18px;font-size:12px;color:var(--muted);text-align:center}
    @media(max-width:880px){
      .container{grid-template-columns:1fr; padding:0}
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="hero">
      <h1>SafePay — Paiement sécurisé (démonstration)</h1>
      <p class="lead">Ceci est une page d’exemple qui montre un formulaire de paiement moderne. Elle n'accepte pas de vraies transactions — parfaite pour apprendre ou tester.</p>

      <div class="features" aria-hidden="true">
        <div class="feature">🔒 Validation côté client</div>
        <div class="feature">🧾 Récapitulatif clair</div>
        <div class="feature">⚠️ Pas de traitement réel</div>
      </div>

      <div style="margin-top:18px">
        <h3 style="margin:0 0 8px">Comment ça marche</h3>
        <p class="muted" style="margin:0">Remplis les champs à droite et clique sur «Payer». Le formulaire valide localement et affiche une confirmation.</p>
      </div>

      <footer>
        Exemple éducatif — n'essaie pas d'imiter des sites officiels.
      </footer>
    </div>

    <div class="card" role="region" aria-labelledby="form-title">
      <h2 id="form-title" style="margin-top:0">Paiement</h2>

      <label for="email">Email</label>
      <input id="email" class="input" type="email" placeholder="ex: amis@example.com" required>

      <label for="name">Nom sur la carte</label>
      <input id="name" class="input" type="text" placeholder="Nom complet" required>

      <div class="row">
        <div class="small">
          <label for="card">Numéro de carte</label>
          <input id="card" class="input" type="text" placeholder="1234 5678 9012 3456" maxlength="19" inputmode="numeric" required>
        </div>
      </div>

      <div class="row">
        <div class="small">
          <label for="exp">Expiration (MM/AA)</label>
          <input id="exp" class="input" type="text" placeholder="08/26" maxlength="5" required>
        </div>
        <div style="width:120px">
          <label for="cvc">CVC</label>
          <input id="cvc" class="input" type="text" placeholder="123" maxlength="4" inputmode="numeric" required>
        </div>
      </div>

      <div style="margin-top:6px">
        <label for="amount">Montant</label>
        <input id="amount" class="input" type="number" min="0.5" step="0.01" value="19.99" required>
      </div>

      <div class="total">
        <div>Totale</div>
        <div id="total-display">€19.99</div>
      </div>

      <button id="payBtn" class="btn">Payer maintenant</button>

      <div class="notice">
        <strong>Note :</strong> page de démonstration — aucune information n'est envoyée à un serveur.
      </div>
    </div>
  </div>

  <!-- modal -->
  <div id="modal" class="modal" role="dialog" aria-modal="true" aria-hidden="true">
    <div class="box">
      <div class="ok">✓</div>
      <h3>Paiement simulé accepté</h3>
      <p class="small-muted" id="summary"></p>
      <div style="margin-top:14px">
        <button onclick="closeModal()" class="btn" style="width:auto;padding:10px 14px">Fermer</button>
      </div>
    </div>
  </div>

  <script>
    // petits utilitaires de validation et formatage (démonstration)
    const email = document.getElementById('email');
    const nameI = document.getElementById('name');
    const card = document.getElementById('card');
    const exp = document.getElementById('exp');
    const cvc = document.getElementById('cvc');
    const amount = document.getElementById('amount');
    const totalDisplay = document.getElementById('total-display');
    const payBtn = document.getElementById('payBtn');
    const modal = document.getElementById('modal');
    const summary = document.getElementById('summary');

    function formatCard(v){
      return v.replace(/\s+/g,'').replace(/(\d{4})/g,'$1 ').trim();
    }
    card.addEventListener('input', e => {
      const pos = e.target.selectionStart;
      e.target.value = formatCard(e.target.value);
    });

    exp.addEventListener('input', e=>{
      let v = e.target.value.replace(/[^\d]/g,'').slice(0,4);
      if (v.length>2) v = v.slice(0,2)+'/'+v.slice(2);
      e.target.value = v;
    });

    amount.addEventListener('input', ()=> {
      let val = parseFloat(amount.value) || 0;
      totalDisplay.textContent = '€' + val.toFixed(2);
    });

    function validate(){
      if (!email.value.includes('@')) return 'Email invalide';
      if (nameI.value.trim().length < 2) return 'Nom invalide';
      if (card.value.replace(/\s/g,'').length < 13) return 'Numéro de carte trop court';
      if (!/^\d{2}\/\d{2}$/.test(exp.value)) return "Date d'expiration (MM/AA) invalide";
      if (cvc.value.length < 3) return 'CVC invalide';
      if (parseFloat(amount.value) < 0.5) return "Le montant doit être ≥ 0.50";
      return null;
    }

    payBtn.addEventListener('click', ()=>{
      const err = validate();
      if (err){
        alert('Erreur : ' + err);
        return;
      }
      // simulation d'un envoi : afficher un modal de succès
      summary.textContent = `Merci ${nameI.value.trim()} — un reçu a été (simulé) envoyé à ${email.value.trim()}. Montant : €${parseFloat(amount.value).toFixed(2)}.`;
      modal.style.display = 'flex';
      modal.setAttribute('aria-hidden','false');
    });

    function closeModal(){
      modal.style.display = 'none';
      modal.setAttribute('aria-hidden','true');
    }

    // fermer modal au clic hors boîte
    modal.addEventListener('click', (e)=>{
      if (e.target === modal) closeModal();
    });

    // accessibilité: Enter pour soumettre quand sur un champ
    [email,nameI,card,exp,cvc,amount].forEach(el=>{
      el.addEventListener('keydown', (e)=>{
        if (e.key === 'Enter') payBtn.click();
      });
    });
  </script>
</body>
</html>
